<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.codewrite.teceme">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".ui.category.CategorySearchable"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/category_searchable" />
        </activity>

        <activity
            android:name=".ui.store.StoreSearchable"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/store_searchable" />
        </activity>
        <activity
            android:name=".ui.product.ProductSearchable"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/product_searchable" />
        </activity>

        <service
            android:name=".service.MyService"
            android:enabled="true"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.store.StoresMapActivity"
            android:label="@string/title_activity_stores_map" />
        <activity android:name=".ui.others.HelpAndSupportActivity" />
        <activity android:name=".ui.others.ConfirmationActivity" />
        <activity
            android:name=".ui.account.ProfileActivity"
            android:label="@string/title_activity_profile" />
        <activity android:name=".ui.checkout.CheckoutActivity" />
        <activity android:name=".ui.wallet.CreateWalletActivity" />
        <activity android:name=".ui.payment.PaymentMethodActivity" />
        <activity
            android:name=".ui.cart.CartActivity"
            android:label="@string/title_activity_cart"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.store.StoreListActivity"
            android:label="@string/title_store_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.store.StoreDetailActivity"
            android:label="@string/title_store_detail"
            android:parentActivityName=".ui.store.StoreListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.codewrite.teceme.ui.store.StoreListActivity" />
        </activity>
        <activity
            android:name=".ui.category.CategoryListActivity"
            android:label="@string/title_category_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.category.CategoryDetailActivity"
            android:label="@string/title_category_detail"
            android:parentActivityName=".ui.category.CategoryListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.codewrite.teceme.ui.category.CategoryListActivity" />
        </activity>
        <activity
            android:name=".ui.product.ProductListActivity"
            android:label="@string/title_product_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.product.ProductDetailActivity"
            android:label="@string/title_product_detail"
            android:parentActivityName=".ui.product.ProductListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.codewrite.teceme.ui.product.ProductListActivity" />
        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.others.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.account.LoginActivity"
            android:label="@string/login_text"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.others.WelcomeActivity"
            android:label="@string/welcome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.account.SignupActivity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>